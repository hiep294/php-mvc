Router:
if($url == "/MVC/")
not ($url == "/mvc/")
 

dòng "Task là động theo url nhập vào." copy sai


dòng "⦁	Dùng design pattern ORM để tạo model TaskModel ánh xạ với bảng task trong database và hàm get "
UML của TaskModel, function set, get thiếu params

"dùng _init($table, $id, $model) để lấy tên bảng và các dữ liệu khác."
code dưới bị sai tên _inni